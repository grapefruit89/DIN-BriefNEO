{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile, API keys, layout settings, and brief draft payload.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Firebase Auth unique User ID"
        },
        "apiKeys": {
          "type": "object",
          "description": "JSON object with custom Geoapify and Google Maps API keys"
        },
        "senderProfile": {
          "type": "object",
          "description": "JSON object with sender address card fields"
        },
        "settings": {
          "type": "object",
          "description": "JSON object with active visual toggle modes and options"
        },
        "letterPayload": {
          "type": "object",
          "description": "JSON object containing current template text and contents of the editor"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO string of the last edit timestamp"
        }
      },
      "required": ["uid"]
    },
    "SharingCode": {
      "title": "SharingCode",
      "description": "Short code mapping translating unique 6-digit sync key to a authenticated User ID.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Reference UID that this short sharing code represents"
        },
        "createdAt": {
          "type": "string",
          "description": "Epoch or ISO string of generation time"
        }
      },
      "required": ["uid", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Storage location for private configurations, credentials, and draft backups."
    },
    "/sharingCodes/{code}": {
      "schema": "SharingCode",
      "description": "Global directory mapping sync codes to active secure User IDs."
    }
  }
}
