# Dependencies & environments
venv/
node_modules/

# Agent / editor local state (nicht Teil des Produktivrepos)
agent/cache/
.claude/
chat-exports/

# Python
__pycache__/
*.pyc

# Datenbanken (lokal generiert, nicht versioniert)
*.db
*.db-journal

# Generierte Agenten-Artefakte liegen unter agent/cache/ (bereits oben ausgeschlossen).
# Root-Reste und autogenerierte Build-Artefakte (verhindert Git-Noise bei Zeilenverschiebungen):
LLM_CONTEXT.md
import.sql
docs/10-architecture/Code-Referenzen.md

# OS / Editor
.DS_Store
Thumbs.db

# Recherche-Rohdaten (14.3 MB ChromeStatus-Dumps nicht ins Git versionieren)
research/research_results/chrome_features_*.json
research/**/__pycache__/
