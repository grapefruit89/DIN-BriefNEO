{
  "version": "1.1.0",
  "layer": "project",
  "description": "DIN-BriefNEO-only Sonden. Keine generische Boilerplate.",
  "rules": [
    {
      "id": "P1",
      "catalog_ref": ["ADR-ANTIPATTERN §0"],
      "class": "preferred",
      "severity": "high",
      "check": "regex",
      "category": "javascript",
      "description": "setHTMLUnsafe() only when unfiltered HTML is an explicit requirement. Default: setHTML() or textContent.",
      "pattern": "setHTMLUnsafe",
      "isRegex": false,
      "file_patterns": ["*.js"],
      "exemptions": [
        {
          "file": "website/js/main.js",
          "reason": "Existing call sites pending review; not a blanket permit to add new uses.",
          "reference": "ADR-ANTIPATTERN §0"
        },
        {
          "file": "website/js/healthcheck.js",
          "reason": "Diagnostics may need raw markup; not a product-path default.",
          "reference": "ADR-ANTIPATTERN §0"
        }
      ]
    },
    {
      "id": "P2",
      "catalog_ref": ["A38"],
      "class": "info",
      "severity": "info",
      "check": "review",
      "category": "network",
      "description": "CDN/asset loading is a Catalog HARD BAN (see base B5, W7). Optional product APIs are not CDNs. Current intended fetch hosts: photon.komoot.io, api.geoapify.com, api.zippopotam.us, myprojects.geoapify.com. www.w3.org is a spec URL, not an API allowlist entry.",
      "pattern": "",
      "file_patterns": ["*.js", "*.html"],
      "exemptions": []
    }
  ]
}
